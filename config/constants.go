package config

import (
	"fmt"
)

const (
	UPLOAD_DIR           = "uploads"
	SCREENSHOT_FILE      = "screenshot.jpeg"
	SCREENSHOT_HASH_FILE = "screenshot_hash.txt"
)

func PrintDebug(message string) {
	// Implement the printDebug function here or import it from the appropriate package
	fmt.Println(message)
}
